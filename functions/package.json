{
  "name": "safelink-functions",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "@google-cloud/firestore": "^8.3.0",
    "firebase-admin": "^11.11.0",
    "firebase-functions": "^4.9.0",
    "nanoid": "^3.3.7"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
